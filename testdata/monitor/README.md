Fixtures for the `internal/monitor` package.

- `session2.jsonl.gz` is a checked-in gzip fixture for the gzip-loading tests.
- `aggregate.golden.json` is the golden snapshot for `AggregateDetail` tests.
