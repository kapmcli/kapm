// Package monitor loads, merges, and aggregates agent session data
// from multiple sources (v2 file store, v1 SQLite, IDE sessions,
// hook logs) and presents it via TUI or JSON output.
package monitor
